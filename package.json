{
  "name": "week-2-wallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "install-packages": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "cd server && cd dist && node index.js",
    "deploy": "npm run install-packages && npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
